<%- include('../partials/header') %>


<ul>
 
  <% skills.forEach(function(s) { %>
    <li>
     
      <a href="/skills/<%= s.id %>"><%= s.name %></a>
      has 
      <%= s.learned ? 'been learned' : 'NOT been learned' %>
    </li>
    <% }); %>
</ul>
<a href="/skills/new">Add skill</a>
  
<%- include('../partials/footer') %>